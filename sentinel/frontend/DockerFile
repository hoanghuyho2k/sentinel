# ---- FRONTEND (React + Vite) ----
FROM node:22-alpine

WORKDIR /app

# Copy dependency files first
COPY package*.json ./

# Install dependencies
RUN npm ci || npm install

# Copy rest of app
COPY . .

# Expose Vite port
EXPOSE 5173

# Run Vite explicitly
CMD ["npx", "vite", "--host"]